# DeepL Voice-to-Voice Lambda Proxy URLs
VITE_GET_LANGUAGES_PROXY=https://blah0hrpsa.lambda-url.eu-west-2.on.aws/
VITE_REQUEST_SESSION_PROXY=https://blah0jotax.lambda-url.eu-west-2.on.aws/

# Optional: Default environment for DeepL API (dev or prod)
# Only affects initial load - users can switch via UI in debug mode (?debug=true)
# VITE_DEEPL_ENVIRONMENT=prod
